# 2026-07-12 工作日志

### 产品说明书文档
- 用户要求生成 PolyAgent 的 Word 说明书（.docx 格式）
- 用 python-docx 库生成，脚本 scripts/gen_docx.py
- 内容包含 12 个章节：产品概述、核心架构、快速开始、CLI 指南、SDK 指南、内置工具一览、技能市场、多 Agent 编排、企业级特性、测试与质量、常见问题、附录
- 输出：PolyAgent_产品说明书.docx
- 用户反馈缺少「首次使用应该从哪个目录打开 CMD」的说明 → 新增「3.2 运行前准备」章节：详细说明 cd 到项目根目录 + set PATH 的方法，以及永久加入 PATH 的步骤。FAQ 中对应的 .env 条目也补充了目录说明。
- 用户反馈说明书中 `set PATH=<venv>/Scripts;%PATH%` 命令无用（占位符） → 已替换为实际完整路径 `C:\Users\孙\.workbuddy\binaries\python\envs\default\Scripts`。

### 简历替换方案修正
- 用户指出方案中"已可用于简历项目展示"是自杀式表述 → 改为"已应用于代码仓库分析、工具编排等企业级场景"
- 恢复 SFT/LoRA 微调和 Milvus/pgvector 向量库（用户要求保留）
- 项目介绍去掉了"双重技能市场""从0层面到底层到交付完整的工程化简历项目"等不合适表述
- 项目经历去量化+合并：10条bullet→6条，去掉"12个工具""89个单测""75%""9个子包"等所有具体数字

### 搜索引擎改造 + Shell 健壮性修复
- **WebSearch 多引擎化**：默认 Bing（国内可用）+ DuckDuckGo + 搜狗，Args 加 engine 字段支持对话切换
- **Shell 异常保护**：LLM API 调用包 try/except 不再炸 shell；Assistant 消息移出工具循环（修 DeepSeek 400）；工具结果截断 2000 字符
- **搜完不回答修复**：工具循环耗尽后自动做一次无工具 LLM 调用，强制生成最终回答
- **搜索策略提示词**：最多搜 3 次，搜不到直接总结，禁止反问用户

### 新增 `polyagent talk` 聊天模式
- 拟人化系统提示词，回答像真人一样有情感、有性格
- 完全隐藏工具调用过程，只输出最终回答
- 默认 temperature 1.0 更活泼
- 新增规则：回答后偶尔自然好奇用户想法，主动抛出问题（但不为要信息才反问）
- **Bing HTML 解析修复**：Bing 把 `<h2>` 改成 `<h2 class="">`，正则改成 `<h2[^>]*>`，现在能正确解析世界杯等实时搜索结果
- **talk 提示词强化**：新增"#1 rule"和示例，要求问起时效性问题必须先搜 web_search，禁止反问用户找补
- 产品说明书 4.4 节同步更新 talk 命令描述，加入"时效性优先"说明
- **DeepSeek DSML 工具调用解析**：DeepSeek 在 content 中返回 `<|DSML| |tool_calls>...<|DSML| |invoke name="...">` 格式，不是标准 tool_calls 字段，导致 talk 模式调不起来。新增 `_parse_dsml` 和 `_strip_dsml` fallback，92 tests 全绿
