# 2026-07-10 工作日志

## PolyAgent 简历项目（纯后端多智能体框架）

### 规划
- 与用户确认：多智能体协作系统 / Python / SDK+CLI / 四大亮点全要 / DeepSeek / Mock 默认 / 代码仓库分析 showcase。
- 产出 PROJECT_PLAN.md（WHY、技术选型、分层架构、M0-M8 里程碑、简历卖点映射、风险取舍）。

### M0 脚手架 ✅
- pyproject.toml(setuptools) + 9 子包 + ruff/mypy/pytest 配置 + GitHub Actions CI(3.11/3.12/3.13)。
- typer CLI `polyagent version`，修了单命令模式坑（加空 @app.callback）。
- 验收：3 tests pass，ruff 全绿，import OK。

### M1 LLM 层 ✅
- core: types.py(Role/Message/Usage/CostReport/ModelPricing/PRICING/AgentSpec)、exceptions.py(LLMError/Retryable/RateLimit/Timeout/ProviderUnavailable/BudgetExceeded)、agent.py(单轮 Agent)。
- llm: provider.py(Protocol)、middleware.py(Retry/RateLimit/Fallback/Budget/CostAccount + build_chain)、client.py(LLMClient)、mock.py(MockProvider 脚本+echo)、deepseek.py(OpenAI 兼容 httpx)。
- 可靠性中间件链设计：限流→重试(指数退避+jitter, Retry-After)→降级(主→备)→预算校验→provider→成本统计。
- 验收：15 tests pass(3 smoke + 4 mock + 6 middleware + 2 agent)，ruff 全绿，离线 Mock 无需 Key。
- 踩坑：ruff isort I001(自动 --fix)、E501 长行拆局部变量。

### 下一步 M2：工具系统（Tool 基类 + registry + pydantic→JSON Schema 自动生成 + 内置工具 + 沙箱）。

### M2 工具系统 ✅
- tools/base.py：Tool(ABC) + ToolResult；`schema()` 用 `args_model.model_json_schema()` 自动生成 OpenAI function-calling schema（零手写）；`call()` 校验后 run。
- tools/registry.py：ToolRegistry(register/get/schemas) + with_builtins()。
- tools/builtins.py：PythonExecute(subprocess+timeout 沙箱)、ReadFile、WriteFile、HttpRequest、GrepFiles(本地搜索)。
- core/types.py 扩展：Message 加 tool_calls；加 ToolCall(id/name/arguments JSON str)。
- llm/types.py 扩展：LLMRequest.tools、LLMResponse.tool_calls。
- core/agent.py 扩展：tool loop（LLM 返回 tool_calls → 执行 → tool message 回传 → 再生成，max_tool_iters 防死循环）。
- llm/deepseek.py 扩展：payload 传 tools、解析 tool_calls、_to_openai 序列化 tool_calls/tool_call_id。
- 验收：27 tests pass(+12: schema/registry/builtins/agent tool loop)，ruff 全绿，schema 自动生成冒烟 OK。
- 踩坑：ruff UP041(asyncio.TimeoutError→builtin TimeoutError)、F401(删未用 import)、E501 描述拆行。

### 下一步 M3：记忆 + RAG（短期/长期记忆、上下文压缩、文档切分/embedding/检索，默认可插拔向量库）。

### M3 记忆 + RAG ✅
- memory/base.py：MemoryRecord + Memory Protocol。
- memory/buffer.py：ConversationBuffer（FIFO maxlen，多轮记忆）。
- memory/vector_memory.py：VectorMemory（embedder+store，长期记忆相似度检索）。
- memory/compressor.py：TokenWindowCompressor（按 token 估测算保留最近）+ SummarizingCompressor（LLM 摘要旧消息成 system msg，保留 keep_recent 条）。
- rag/embedder.py：Embedder Protocol + HashEmbedder（确定性归一化向量，**无真实语义，仅测试/演示流程**，生产换真实 embedder）。
- rag/vectorstore.py：VectorStore Protocol + InMemoryVectorStore（余弦线性扫描，可插 chromadb/pgvector）+ Hit TypeAlias。
- rag/splitter.py：TextSplitter（chunk_size+overlap，校验 overlap<size）。
- rag/index.py：Document + RAGIndex（split→embed→store→query）。
- 验收：38 tests pass(+11: buffer/vector_memory/compressor/embedder/vectorstore/splitter/ragindex)，ruff 全绿，离线零外部依赖。
- 踩坑：E501 长注解用 TypeAlias(Hit) + 长表达式拆局部变量。

### 下一步 M4：编排器（Planner→Workers(并行)→Critic(评审回退)→Synthesizer，任务 DAG + 并发 + 模型降级）。

### M4 编排器 ✅
- orchestration/types.py：TaskStatus(pending/running/done/failed)、TaskNode(id/description/deps/status/result/attempts)、Critique(accepted/feedback)、RunResult(answer/task_graph/latency/total_attempts/estimated_cost)。
- orchestration/roles.py：Planner(goal→JSON task list)、Worker(执行 task)、Critic(JSON accept/feedback，容错解析 ```json 包裹)、Synthesizer(汇总 done tasks)。_extract_json 容错。
- orchestration/orchestrator.py：_schedule DAG 拓扑调度（ready=deps 全 DONE）、asyncio.Semaphore 限并发、_run_task 评审回退（max_review_retries，reject 则重做，耗尽标 FAILED）、失败依赖阻断（deps FAILED → 依赖者标 FAILED 不执行）。run 返回 RunResult。cost 通过共享 CostReport（可选注入）。
- 模型降级/重试/成本复用 M1 的 LLMClient 中间件，编排层不重复实现。
- 验收：42 tests pass(+4: planner 解析/全流水线/评审回退/失败阻断)，ruff 全绿。
- 关键设计：四角色流水线 + DAG 调度 + 评审回退，是简历「多智能体协作架构」核心落点。

### 下一步 M5：可观测性（tracer span 树 + metrics + structlog + 成本统计）。

### M5 可观测性 ✅
- observability/tracer.py：Span(dataclass) + Tracer（@contextmanager + contextvars 维护当前 span 栈，自动嵌套；error 标记+重抛；to_dict 序列化 span 树含 duration_ms/status/children）。
- observability/metrics.py：Metrics（inc 计数器 + observe 计时 + summary 聚合 count/avg/max）。
- observability/logging.py：structlog configure_logging（ISO+level+ConsoleRenderer）+ get_logger。
- observability/report.py：RunReport（answer/trace/metrics/cost/latency）。
- observability/middleware.py：ObservabilityMiddleware（LLM 层自动埋点 llm.chat span + 计 llm.calls/tokens/errors + 日志）。
- Orchestrator 加 tracer 参数 + _span(nullcontext 兜底) + 埋点 orchestrator.run/planner.plan/schedule/synthesizer.synthesize/task/worker.execute/critic.review。
- 端到端验证：一次 run 产出 trace(span 树)+metrics(llm.calls=6)+cost(deepseek-chat 定价>0)。
- 验收：46 tests pass(+4: tracer 嵌套/错误/metrics/端到端)，ruff 全绿。
- 踩坑：`Any` 在 typing 不在 collections.abc（手误 import，运行时 ImportError）；UP037 去引号、SIM117 合并 with 自动修。

### 下一步 M6：CLI（typer run/chat/eval 命令串联完整流水线）。

### M6 CLI ✅
- cli/demo.py：build_demo_orchestrator()——脚本化 MockProvider 装配四角色（planner 返回固定 2-task DAG JSON、worker echo、critic default accept、synth default 占位），离线可跑完整 Plan→Workers→Critic→Synth。
- cli/__init__.py：version / run(goal 位置参数 + --provider mock|deepseek) / chat(交互 stdin) / eval(M7 占位)。run 输出 Answer + task graph(含 status/attempts) + cost/latency/trace 摘要。
- 验收：50 tests pass(+4: version/run 实跑/拒绝未知 provider/eval 占位)，ruff 全绿。`polyagent run "build a small web app"` 实跑输出 t1/t2 done + Answer + Trace roots:1。
- 踩坑：typer Argument 是位置参数，测试不能带 --goal（exit 2）；E501 拆 planner_resp 局部变量。

### 下一步 M7：Eval（数据集 + 评分器 + 回归基线）。

### M7 Eval ✅
- eval/types.py：EvalCase(id/input/expected)、EvalResult(passed/score/output/detail)、EvalReport(results/n/passed/pass_rate/avg_score，build 类方法聚合)。
- eval/scorers.py：Scorer 协议 + ExactMatchScorer(trim 后全等) + ContainsScorer(子串包含)。可插拔 LLMJudgeScorer 后续。
- eval/dataset.py：Dataset + default() 3 用例（c1/c2 pass、c3 fail 演示 pass_rate 计算）。
- eval/runner.py：EvalRunner(subject, scorer)，subject=async(input)->str 通用（Agent/Orchestrator/任意函数），跑数据集→评分→EvalReport。runner 与被测解耦。
- cli eval 命令：真实实现，跑默认数据集 + ContainsScorer + echo agent subject，输出 pass 率 + 逐条 PASS/FAIL。
- 验收：55 tests pass(+5: scorers/dataset/runner/CLI eval)，ruff 全绿。`polyagent eval` 实跑输出「2/3 passed (67%), avg_score=0.67」+ c1/c2 PASS c3 FAIL。
- 踩坑：M6 eval 占位测试断言 "M7" 需同步更新为真实输出。
- **四大简历亮点全齐**：LLM 编排可靠性(M1) / 工具调用(M2) / 记忆与 RAG(M3) / 可观测性(M5)+评测(M7)，多智能体架构(M4)。

### 下一步 M8：文档 + 代码仓库分析示例（README 简历亮点段 + ARCHITECTURE + showcase）。

### M8 文档 + 示例 ✅
- README.md：完整版（Why/Install/Quick start 真实命令 run/eval/chat/SDK 示例/架构/Roadmap 全✅/showcase/简历亮点段/测试）。含简历一句话叙事。
- docs/ARCHITECTURE.md：更新为「全部已实现」+ 数据模型对齐实际字段（Message/ToolCall/AgentSpec/TaskNode/RunResult）+ 加 Eval 段。
- examples/repo_analysis/analyze.py：代码仓库分析 showcase（Planner 拆 inv/entry/tests/smells/report 5-node DAG、Worker 持 grep_files+read_file、Critic、Synth）。Mock 离线跑通（5 tasks/5 attempts/1 trace root）+ showcase README。
- overview.md：项目总览（做了什么/关键决策/里程碑表/文件结构/亮点映射/可扩展边界）。
- 验收：ruff 全绿、55 tests pass、`python examples/repo_analysis/analyze.py .` 跑通。
- 踩坑：E501 PLAN 字符串 smells 行拆拼接。

### 🎉 项目完成（M0–M8 全交付）
- 55 tests passing，ruff 全绿，全程离线 Mock，SDK + CLI 交付，纯后端无 UI。
- 四大简历亮点全齐 + 多智能体架构。
- 后续可扩展（明确边界，未做）：DeepSeek 真实联调、真实向量库(chromadb/pgvector)、LLMJudgeScorer、真实 embedding。

---

## 企业级补强（M9–M14）

### M9 真实可用性 ✅
- polyagent/config.py：pydantic-settings 读 .env（DEEPSEEK_API_KEY/model/base_url，env_prefix=DEEPSEEK_，lru_cache）。
- llm/deepseek.py 加 stream() async iterator（stream=True，解析 data: SSE delta，[DONE] 结束）。
- llm/client.py 加 stream()（getattr provider.stream，直连跳 middleware）。
- llm/mock.py 加 stream()（echo 按词 yield，离线流式测试）。
- core/agent.py 加 run_stream()（无工具循环，直连 client.stream yield）。
- cli/demo.py 加 build_deepseek_orchestrator()（真实 DeepSeek 四角色，client 挂 ObservabilityMiddleware+CostAccountMiddleware）。
- cli run 加 --provider deepseek 分支；cli chat 加 --provider deepseek --stream。
- 真实联调：`polyagent run --provider deepseek "..."` → Answer 真实、Cost **$0.000763>0**（cost 真实统计）、Latency 8.7s；`printf "..." | polyagent chat --provider deepseek --stream` → 流式输出。
- 验收：ruff 全绿、55 tests pass、真实 DeepSeek 联调通过（cost 真实 + 流式工作）。
- 踩坑：build_deepseek 闭包 _client 引用 tracer/cost 但定义在末尾 → NameError（free variable 未绑定）；修：tracer/cost 提前到 _client 前。test_run_rejects_unknown_provider 用 deepseek 会真实联网 → 改 unknown。CLI 长行拆局部变量。
- 安全：DeepSeek key 贴在对话里了，提醒用户联调后轮换。

### 下一步 M10：持久化（sqlite 存 run/task/trace + CLI 历史查询）。

### M10 持久化 ✅
- persistence/store.py：RunStore（sqlite3，建 runs/tasks/traces 三表；save 存 run+task_graph+trace 返回 run_id；list 倒序；get 取 run+tasks+trace；stdlib 无依赖）。
- persistence/__init__.py 导出（M0 没建 persistence 目录，store.py 是新目录+__init__ 新建）。
- cli run 加 --persist(默认 True)：跑完存 polyagent.db 输出 Run ID；加 `polyagent runs`（列历史 id/cost/latency/time/goal）+ `polyagent show <id>`（answer+task graph+trace 摘要）。
- .gitignore 加 polyagent.db/*.db。
- 验收：58 tests pass(+3: save/list/get、missing、多 run 倒序)，ruff 全绿。联调 `polyagent runs` + `show <id>` 跑通。
- 踩坑：`datetime.UTC` 是 module 属性不是类属性（`from datetime import UTC` + `datetime.now(UTC)`，非 `datetime.UTC`）；created_at 秒精度同秒排序不稳 → 用完整微秒 isoformat()；test_run 加 --no-persist 避免污染项目根 db。

### 下一步 M11：可观测后端（OTLP trace 导出 + Prometheus metrics + docker-compose 验证）。

### M11 可观测后端 ✅
- observability/exporters.py：export_traces_to_otlp(tracer, endpoint)——遍历 span 树递归创建 OTel span（start_as_current_span + set_span_in_context 嵌套），设 attributes/status，SimpleSpanProcessor + OTLPSpanExporter(otlp-proto-http) force_flush 导出；export_metrics_to_prometheus(metrics, pushgateway)——prometheus_client push_to_gateway。
- config.py 加 ObservabilitySettings（otel_exporter_otlp_endpoint / prometheus_pushgateway，读 .env）。
- observability/__init__.py 加 lazy export_traces_to_otlp/export_metrics_to_prometheus（重依赖 optional）。
- cli run 跑完：若配 OTEL endpoint 调 export_traces_to_otlp；若配 pushgateway 构造 run 级 metrics(runs/latency/cost) push。try/except ImportError 提示装 `.[observability]`。
- pyproject 加 [observability] extra（opentelemetry-sdk/exporter-otlp-proto-http/prometheus_client）。
- docker-compose.yml：jaeger all-in-one(4318 OTLP + 16686 UI) + pushgateway + prometheus + observability/prometheus.yml。
- 验收：60 tests pass(+2: OTLP export 计数 span / empty)。ruff 全绿。
- 踩坑：**循环 import**（core.__init__ 顶层 import agent→llm.client，而 llm→core 触发 core.__init__ 部分初始化）——M1 埋下靠 pytest 顺序没炸，python -c 暴露；修：core.__init__ 改 PEP562 lazy `__getattr__` 延迟 Agent import。ruff SIM117 合并 with 改语义（嵌套 span 变平级）——测试不依赖嵌套故接受。
- **OTel exporter 不发 HTTP 的真 bug**：`trace.get_tracer()` 用全局 NoOp provider，不进我建的 provider/processor → 不导出。修：`provider.get_tracer("polyagent")` 直接绑定 + 加 `Resource.create({"service.name":"polyagent"})`。修后 mock server hits=2 body_len=300。
- **pytest 128s→3s**：.env 配了 OTEL endpoint，每个 cli run 测试都连 localhost:4318 重试 ~3s。修：.env 移除 OTEL endpoint（默认不导出，真验证用环境变量 `OTEL_EXPORTER_OTLP_ENDPOINT=...`）。
- **真 Jaeger 验证通过** ✅：`docker run jaegertracing/all-in-one:1.60`（代理开后拉镜像成功）+ `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces polyagent run "..." --provider mock` → **Exported 10 spans to OTLP**；Jaeger `/api/services` 返回 `{"data":["polyagent"]}`；`/api/traces?service=polyagent` 返回真实 trace（traceID + spans，operationName "planner.plan" + CHILD_OF 父子关系 + tags otel.library.name=polyagent）。span 树端到端打通。

### 下一步 M12：RAG 真实化（DeepSeek embedding API + ChromaVectorStore）。

### M12 RAG 真实化 ✅（2026-07-11）
- **DeepSeek embedding 实测 404**：curl `https://api.deepseek.com/v1/embeddings` 返回 404——DeepSeek 官方只支持 chat，不支持 embedding（百度云文章误导）。改用 fastembed（本地 ONNX 真实语义，轻量 ~100MB，不需 torch）。
- rag/embedder.py 加 FastEmbedEmbedder：用 fastembed.TextEmbedding（默认 paraphrase-multilingual-MiniLM-L12-v2 多语言）；lazy import + asyncio.to_thread 包装同步 embed；dim property lazy 探测。HashEmbedder 保留为离线测试默认。
- rag/vectorstore.py 加 ChromaVectorStore：chromadb.PersistentClient 持久化；add 用 upsert（避免重复 id 报错）；query 返回 (id, 1/(1+distance), meta) 把 L2 distance 映射 (0,1] 相似度。InMemoryVectorStore 保留测试默认。
- rag/__init__ 导出 FastEmbedEmbedder/ChromaVectorStore；pyproject [rag] extra 加 fastembed。
- .gitignore 加 chroma/、chroma_*/（持久化向量库不提交）。
- 真实联调 ✅：FastEmbed 下 model(~30s) + ChromaVectorStore add 3 文档（Python 编程×2 + 天气×1）+ query「编程语言 Python」→ **d2(0.43) > d1(0.28) > d3(0.026)**，编程相关 score 远高于天气，语义排序正确。assert PASS。
- 验收：60 tests pass（HashEmbedder/InMemory 测试不受影响），ruff 全绿。
- 踩坑：chromadb 默认 L2 distance，score 负数不直观 → 改 1/(1+distance) 映射 (0,1]。

### 下一步 M13：沙箱强化（Docker 容器执行 PythonExecute + 路径白名单）。
