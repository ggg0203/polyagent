# 2026-07-13 工作日志

### 长期记忆（跨会话 ChatStore）
- 用户询问 shell/talk 模式是否有跨会话长期记忆 → 新增 polyagent/persistence/chat_store.py
- ChatStore：SQLite 持久化对话历史，按 mode 区分 shell/talk，自动复用当天 session
- 启动时加载上次对话最后 20 条记录作为 SYSTEM 上下文注入提示词
- 每次用户交互后自动保存 messages 到 SQLite
- 集成到 cli/__init__.py 的 _shell_async 和 _talk_async
- 3 tests for ChatStore + 4 tests for DSML 解析 = 7 新增，95 total passed
