# 2026-07-13 工作日志

### 长期记忆（跨会话 ChatStore）
- 用户询问 shell/talk 模式是否有跨会话长期记忆 → 新增 polyagent/persistence/chat_store.py
- ChatStore：SQLite 持久化对话历史，按 mode 区分 shell/talk，自动复用当天 session
- 启动时加载上次对话最后 20 条记录作为 SYSTEM 上下文注入提示词
- 每次用户交互后自动保存 messages 到 SQLite
- 集成到 cli/__init__.py 的 _shell_async 和 _talk_async
- 3 tests for ChatStore + 4 tests for DSML 解析 = 7 新增，95 total passed

### 产品定位 + 发布
- 分析投资人/面试官视角的 PolyAgent 定位：企业级 AI 中间件（非 Claude Code 竞品）
- 三大差异支柱：可靠性中间件链、可观测+可运维、安全沙箱执行
- 在外部项目验证 `pip install -e` SDK 使用（Mock 离线可跑、DeepSeek 需 API key）
- 创建 GitHub 仓库 https://github.com/ggg0203/polyagent 并推送
- 修改 pyproject.toml 兼容 PyPI 规范（urls、classifiers、license）
- 发布到 PyPI：包名 `scgnb666`，import 仍为 `polyagent`
