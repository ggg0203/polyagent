# 2026-07-11 工作日志

## PolyAgent 企业级补强

### M13 沙箱强化 ✅
- tools/sandbox.py：DockerSandbox（docker run --rm --network=none --memory=256m --cpus=0.5 python:3.12-slim python -c code；asyncio.create_subprocess_exec 调 docker CLI；timeout 杀进程；FileNotFoundError 兜底 docker 未装）+ PathGuard（路径白名单，resolve + relative_to 校验防越狱）。
- tools/builtins.py PythonExecute 加 __init__(sandbox=None)：sandbox 非 None 用 DockerSandbox.run，否则走原 subprocess。默认 subprocess 保持离线测试兼容（with_builtins 用 PythonExecute()）。
- tools/__init__ 导出 DockerSandbox/PathGuard。
- tests/test_sandbox.py：PathGuard 允许/拒绝（tmp_path）、PythonExecute subprocess 默认、DockerSandbox 跑代码（skipif no docker）、PythonExecute(sandbox=DockerSandbox) 注入。**真 docker 联调 5 passed**（拉 python:3.12-slim 镜像 + 容器执行 print）。
- 验收：65 tests pass（60+5），ruff 全绿。docker 测试镜像缓存后 3.37s 全量。
- 踩坑：sandbox.py import Any 未用（F401，删）。
- 设计取舍：默认 subprocess（离线可测、快），DockerSandbox 可选注入（企业级不可信代码隔离）。--network=none 断网、--memory/--cpus 限资源、--rm 自清理、不 mount 主机 → 容器内代码碰不到主机文件系统。

### 下一步 M14：质量收口（mypy strict + 覆盖率门禁 + eval 升级 LLMJudgeScorer + Dockerfile/docker-compose 部署）。

### M14 质量收口 ✅
- mypy strict：python_version 3.12（避免 numpy stubs 3.12 syntax 错）；disable_error_code=["override"]（Tool 子类特化 run(args:SpecificModel) vs 基类 BaseModel 的已知 Liskov 取舍）；修 6 错（logging get_logger 返回 Any、PythonExecute sandbox 类型 DockerSandbox|None 消 Returning Any、vectorstore chromadb to_thread arg-type ignore + None indexable `or [[]][0]` + dict(m) 转 Mapping→dict）。**mypy strict 0 错，49 文件全绿**。
- pytest-cov：pyproject addopts `--cov=polyagent --cov-report=term-missing --cov-fail-under=75`。coverage **77.17%** > 75 门禁。
- eval 升级：LLMJudgeScorer（sync httpx 调 DeepSeek，prompt 让模型输出 JSON {score:0|1,reason}，_extract_json 容错解析，异常返回 0.0；返回类型 Any 避 mypy Any-return）。
- Dockerfile：python:3.12-slim + pip install -e ".[dev,observability]" + ENTRYPOINT polyagent。.dockerignore 排除缓存/.env/chroma/db。
- docker-compose 加 polyagent 服务（profile app，build . + env_file .env + entrypoint polyagent）：`docker compose --profile app run --rm polyagent run "..."`。
- CI：去 `mypy || true`（强制 strict）；装 [dev,observability]；pytest 含 cov 门禁。
- 验收：mypy 0 错 + ruff 全绿 + 65 tests pass + coverage 77%。
- 踩坑：json.loads 返回 Any，mypy strict 不允许 Any-return（即使声明 dict[str,Any]）→ 返回类型用 Any。chromadb stubs 类型严格（ndarray/Sequence/Mapping），to_thread 参数 ignore + dict(m) 转。

### 🎉 企业级补强全完成（M9–M14）
- 65 tests，mypy strict 全绿，coverage 77%，ruff 全绿。
- 真实联调全通过：DeepSeek run cost $0.000763、流式 chat、Jaeger span 树(10 spans)、FastEmbed 语义检索(编程>天气)、Docker 沙箱容器执行。
- 项目从「框架完整+离线 Mock」升级到「企业级：真实 LLM + 持久化 + 可观测后端 + 真实 RAG + Docker 沙箱 + 类型/覆盖率门禁 + 容器化部署」。

### 使用侧补充：SDK 演示脚本
- 用户在用 `polyagent chat` 时只看到普通对话，疑惑框架是否只能聊天。
- 解释：`chat` 是单 Agent 无工具的 demo；框架核心是多 Agent 编排 + 工具 + 记忆 + RAG + 可观测。
- 新增 `examples/sdk_demo.py`：展示单 Agent / 单 Agent+工具 / 多 Agent 编排流水线，Mock 离线可跑，输出 Trace 树和任务 DAG。
- 新增 `examples/tool_demo.py`：展示 Agent 的 `python_execute` 工具循环，真正执行 Python 代码（计算 1+...+100=5050）。
- 记录：当前 `polyagent chat` 未挂载工具，所以和普通 LLM 对话一样；要发挥工具能力需写 Python 代码调用 `Agent(..., tools=with_builtins())`。

### 新增：polyagent shell 命令 + WebSearch 工具
- 用户要求"命令行窗口通过命令让智能体联网搜索、下载技能、读写文件、智能决策"
- **新增 WebSearch 工具** (`polyagent/tools/builtins.py`)：基于 DuckDuckGo lite，免费无需 Key，解析 HTML 返回标题+摘要+URL
- **新增 shell 命令** (`polyagent/cli/__init__.py`)：DeepSeek + 全部内置工具（web_search/python_execute/read_file/write_file/http_request/grep_files），系统提示词指导 LLM 智能决策何时调用何工具，实时显示工具调用过程（⚡调用→✅结果）
- 更新 `with_builtins()` 注册 6 个工具（含 WebSearch）
- 更新 tests：建 69 tests，覆盖率 75.27% > 75% 门禁，ruff 全绿
- 修了大量测试细节（CLI persist/runs/list 测试、WebSearch 解析单元测试）
- 使用方式：`polyagent shell`（需要项目目录内有 .env + DEEPSEEK_API_KEY）

### 新增：技能市场系统（marketplace + installer）
- 用户要求"让 Agent 绑定技能市场，下载任意技能"
- **创建 polyagent/skills/** 包：registry（内建注册表3个技能）+ installer（安装/卸载/动态加载）
- **3 个内建示例技能**：
  - weather: 查询城市天气（北京 28°C 晴等预置数据）
  - file_analyzer: 文件分析（大小/行数/字数/编码检测 UTF-8/GBK/Shift-JIS）
  - datetime_utils: 时间工具（当前时间/日期差计算/时间戳格式化）
- **新增 2 个工具**：marketplace_search（搜索技能仓库）+ marketplace_install（安装技能）
- **shell 命令增强**：启动时自动加载 ~/.polyagent/skills/ 下的已安装技能，注册为可用工具
- 系统提示词增加技能市场说明：当用户需求超出当前工具时，搜索并安装技能

### 新增：腾讯 SkillHub 技能市场集成
- 用户要求"安装 https://skillhub.tencent.com 官网的技能"
- 发现 SkillHub 有公开 REST API：search (api.skillhub.cn/api/v1/search) + download (COS ZIP)
- **新增 3 个工具**：skillhub_search / skillhub_install(slug) / skillhub_list
- 实现：直接 HTTP 调用 SkillHub API，无需安装 CLI；搜索返回技能名/描述/作者/标签；下载 ZIP 解压到 ~/.polyagent/skillhub-skills/{slug}/ 含 SKILL.md
- shell 命令提示词增加 SkillHub 说明：76,000+ 技能可用
- 测试：87 tests pass，覆盖率 75.26% > 75%
- 测试：82 tests pass，覆盖率 76% > 75%

### 新增：从 SkillHub 官方 prompt 安装技能
- 用户截图显示 SkillHub 页面提供"复制 prompt"按钮，想粘贴 prompt 就能安装
- 新增工具 `skillhub_install_from_prompt(prompt)`：从官方 prompt 中自动提取 skill slug，支持中文"安装 xxx"、英文"install xxx"、URL path 等模式
- 修复 http_request 工具未捕获网络异常导致 shell 崩溃的问题（加 try/except）
- 加强 shell 系统提示词：明确要求"当用户要求搜索 SkillHub 技能时，必须直接调用 skillhub_search，禁止用 web_search 搜 SkillHub 官网"
- 测试：89 tests pass，覆盖率 75.03% > 75%

### 简历替换方案
- 用户提供个人简历（C:\Users\孙\Desktop\个人简历第一版.docx），要求删除「医药研发知识管理」项目替换为 PolyAgent
- 输出：简历替换方案.md，含 6 章节完整替换指南（技术栈删增清单/项目经历文案/工作经历调整/统计对比表）

