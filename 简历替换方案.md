# 简历修改方案

> 用途：打开 `个人简历第一版.docx`，对照本文档手动替换内容。
>
> 变更概要：删除「医药研发知识管理与智能文档平台」项目，替换为「PolyAgent」项目；
> 技术栈和工作经历做相应调整。

---

## 一、技术栈变更清单

### 🔴 删除的技术（因医药研发项目移除）

以下技术出现在原简历中，与本项目无关，可从技术栈概述中**移除**（仅限与该项目强绑定的描述，如该技术仍在你其他项目中使用则保留）：

| 技术/描述 | 所属位置 | 处理方式 |
|-----------|----------|----------|
| **LlamaIndex** 文档解析与 Rerank 重排 | AI 应用开发段 | 删除（本项目用 FastEmbed + ChromaDB，无需 LlamaIndex） |
| **LangSmith** 可观测性与 **RAGAS** 评估 | AI 应用开发段 | 删除（本项目用 OTel/Prometheus/LLMJudgeScorer） |
| **Milvus** / **pgvector** 向量数据库 | AI 应用开发段 | 保留（向量数据库为通用技能，项目中已包含 ChromaDB / Milvus / pgvector 多种） |
| 对 **SFT / LoRA** 微调流程有了解 | AI 应用开发段 | 保留（微调经验为通用 AI 能力，跨项目可迁移） |
| **MinIO** 对象存储 | — | 伴随项目删除，技术栈中从未出现过，无需处理 |
| **Socket.IO** | — | 从未出现在技术栈概述中，无需处理 |
| **Kafka / RabbitMQ** 消息队列 | 后端段 | 可以保留（你其他项目用过） |
| **gRPC** 协议 | 基础素养段 | 可以保留（你其他项目用过） |

### 🟢 新增的技术（因 PolyAgent 项目加入）

以下技术应加入技术栈，建议放在「AI 应用开发」和「后端」段：

```
- 自研多智能体编排框架 PolyAgent：从零搭建 Agent/Tool/编排层三层架构，
  支持 Planner→Workers→Critic→Synthesizer 流水线、DAG 拓扑调度、失败阻断与成本统计，
  模型无关（Mock/DeepSeek/OpenAI 兼容），内置中间件链（限流→重试→降级→预算→成本）
- 可观测体系：OpenTelemetry（OTLP/Jaeger）+ Prometheus pushgateway 双端导出
- 质量门禁体系：mypy strict 零容忍 + ruff lint + pytest-cov 覆盖率门禁（75%）
- FastEmbed（ONNX 本地语义模型）+ ChromaDB（持久化向量存储）
- python-docx 文档生成
- pydantic + typer 构建 CLI 框架
- httpx async HTTP 客户端
```

---

## 二、技术栈替换文案

> 找到「AI 应用开发」段落，把这段**替换掉**原来的内容。

### 原内容（删除）

```
AI 应用开发：基于 LangChain / LangGraph 构建 RAG 与多 Agent 系统，熟悉 LlamaIndex 文档解析与 Rerank 重排；Function Calling / MCP / Skill 工具调用与 Prompt Engineering 为日常工作范式，向量数据库（Milvus / pgvector / Chroma）与 Embedding 已在项目中跑通；LangSmith 可观测性与 RAGAS 评估体系有实践经验，VibeCoding / AI 辅助编程为高频工作方式，对 SFT / LoRA 微调流程有了解。
```

### 新内容（粘贴替换）

```
AI 应用开发：基于 LangChain / LangGraph 构建 RAG 与多 Agent 系统，同时自研多智能体编排框架 PolyAgent（从零搭建 Agent / Tool / 编排层、Plan→Execute→Critique→Synthesize 流水线、DAG 拓扑调度、模型无关接入、内置中间件链），已应用于代码仓库分析、工具编排等企业级场景；Function Calling / MCP / Skill 工具调用与 Prompt Engineering 为日常工作范式，向量数据库（ChromaDB / Milvus / pgvector）与 Embedding（FastEmbed ONNX 本地模型）有真实项目落地；可观测体系（OpenTelemetry / Jaeger / Prometheus）与质量门禁（mypy strict / ruff / pytest-cov）有实践经验；VibeCoding / AI 辅助编程为高频工作方式，对 SFT / LoRA 微调流程有了解。
```

---

## 三、项目经历替换

> 找到「项目经历」中「2025.02-2026.07 医药研发知识管理与智能文档平台」整块，**全部删除**，粘贴以下内容替换。

```
2025.02-2026.07  PolyAgent — 模型无关的多智能体协作框架
项目负责人 / 全栈开发工程师

项目介绍：面向企业级多 Agent 系统的纯后端编排框架（SDK + CLI），提供 Planner→Workers→Critic→Synthesizer 完整流水线，内置可靠性中间件链与 RAG 子系统（FastEmbed ONNX 语义模型 + ChromaDB 持久化存储），支持 Docker 沙箱隔离执行、可观测导出（Jaeger / Prometheus）、自建技能注册表与第三方 SkillHub 集成，覆盖从架构设计到 CLI/SDK 交付的完整工程化实践。

技术栈：Python 3.13 + asyncio + httpx + pydantic + typer + fastembed(ONNX) + ChromaDB + opentelemetry-sdk + prometheus-client + pytest + ruff + mypy(strict) + Docker(python:3.12-slim) + SQLite + SkillHub REST API

● 从零设计分层架构（core/llm/tools/orchestration/observability/eval/persistence/cli/skills），多个子包职责清晰，完整体现工程化纵深与架构能力
● 实现编排流水线（Planner→Workers 并发→Critic 评审回退→Synthesizer 汇总），DAG 拓扑调度 + 失败阻断 + 成本统计，体系化解决多 Agent 协作中的上下文膨胀、单点不可靠、不可观测三大痛点；并构建可靠性中间件链（限流→重试→降级→预算→成本），热插拔可组合
● 自研 Tool 工具系统：内置多种工具（python_execute / read_file / write_file / http_request / grep_files / web_search 等），pydantic schema 自动生成 OpenAI function-calling 格式，支持自定义扩展；同时集成自建技能注册表与第三方 SkillHub，Agent 可在对话中搜索安装技能
● RAG 子系统：FastEmbed ONNX 本地语义模型 + ChromaDB 持久化向量存储 + 递归字符文本分块，全离线可用，不依赖外部 API
● 企业级特性：contextvars 无侵入 Span 树 → OTLP 导出到 Jaeger，Prometheus pushgateway 推送 metrics；Docker 沙箱容器隔离执行不可信代码（--network=none --memory --cpus），PathGuard 路径白名单防越狱
● 质量体系：多种 Scorer（ContainsScorer / ExactMatchScorer / LLM Judge），EvalRunner 一键评估；mypy strict + ruff + pytest-cov + CI 门禁全链路自动化
```

---

## 四、工作经历调整

> 找到「工作经历」中「上海互软信息科技有限公司」这一块，只改一处。

### 原内容（改前）

```
以技术负责人角色主导多个企业客户数字化项目的全栈交付，覆盖医药研发、金融合规、物流调度、运营可视化等领域
```

### 修改后

```
以技术负责人角色主导多个企业客户数字化项目的全栈交付，覆盖金融合规、物流调度、运营可视化等领域；同时自研落地多智能体编排框架 PolyAgent（模型无关 Agent 框架，SDK + CLI）
```

---

## 五、项目统计变更

| 维度 | 医药研发项目 | PolyAgent 项目 |
|------|-------------|----------------|
| 项目定位 | 企业级业务平台（Vue+Node+LangChain） | 纯后端智能体框架（Python SDK+CLI） |
| 前端技术 | Vue 3 + TypeScript | 无前端（纯后端） |
| 后端技术 | NestJS + Node.js | Python 3.13 + asyncio |
| AI 框架 | LangChain + LangGraph | 自研 Agent/Tool/编排层 |
| 向量库 | Milvus | ChromaDB + FastEmbed ONNX |
| 消息队列 | Kafka | 无（纯同步 + asyncio） |
| 可观测 | LangSmith + RAGAS | OTel/Jaeger + Prometheus |
| 部署 | K8s + Docker Compose | pip install + CLI |
| 测试 | 未强调 | mypy strict + pytest-cov 75% |
| 简历亮点 | 企业级业务系统落地 | 完整框架设计 + 工程化纵深 |

---

## 六、汇总：技术栈增删明细

### 删掉的技术（因项目替换，且技术栈中仅在该项目有用）

| 技术 | 原因 |
|------|------|
| LlamaIndex 文档解析 | 本项目用自研 splitter |
| LangSmith / RAGAS | 本项目用 OTel / Prometheus / LLM Judge |
| Milvus 向量库 | 保留（仍出现在技术栈举例中） |
| SFT / LoRA 微调提及 | 保留（微调为通用 AI 能力） |

### 新增的技术

| 技术 | 加入位置 |
|------|---------|
| PolyAgent 自研多智能体编排框架 | AI 应用开发段 |
| OpenTelemetry / Jaeger / Prometheus | AI 应用开发段 |
| mypy strict / ruff / pytest-cov 门禁 | AI 应用开发段（或工程化段） |
| FastEmbed ONNX 本地语义模型 | AI 应用开发段 |
| ChromaDB 持久化向量存储 | 已在，增强描述 |
| httpx async HTTP | 后端段 |
| typer CLI 框架 | 后端段 |

### 保持不变的技术

以下技术虽在本项目中未使用，但你其他项目中有，**保留**：
- Vue 3 / React / TypeScript / NextJS
- Node.js / NestJS / Spring Boot
- Kafka / RabbitMQ
- gRPC
- Docker / K8s
- GitHub Actions / Jenkins
